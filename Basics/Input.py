"""

To take input from the user, we can use the input() function.

The input() function takes a string as an argument and returns the input provided by the user as a string.

# Example:
num1 = input("Enter a number: ")
num2 = input("Enter another number: ")
print("The sum is: ", num1 +num2) # This will concatenate the two strings instead of adding them as numbers.

So, to take input as numbers, we need to convert the input to the desired data type using the int() or float() function.
# Example:
num1 = int(input("Enter a number: "))
num2 = int(input("Enter another number: "))
print("The sum is: ", num1 + num2) # This will add the two numbers.


"""